# 현재 프로세스의 작업 디렉토리 확인
import os
cur_dir = os.getcwd()

#현재 작업파일의 경로에 있는 리스트 확인 -> 리스트로 반환
dir_list = os.listdir(cur_dir)

#특정 경로의 폴더(디렉토리) 생성
os.mkdir(cur_dir + '\\dataset_ver2') # or '/dataset'

#지정한 파일의 이름을 변경
os.rename("./test.txt", "test2.txt")
